# git-multi-identity
Separate SSH keys + git author identity for multiple git hosts (e.g. company GitHub Enterprise vs. personal GitHub/GitLab), driven by one config file. Portable across macOS, Linux, and Windows (Git Bash or WSL). Built on standard, open-source tools only: git, ssh-keygen, age. No proprietary agent required.
